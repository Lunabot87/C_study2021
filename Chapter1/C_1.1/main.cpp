#include <iostream> //i(nput)o(utput)stream 
                    //전처리기(preprocessor directive)

int main(void){
    (1 + 2) * (3 + 4); //expression(수식)

    int x = 2;  //정수형(int) 변수(x)를 선언
                //정수형데이터를 담아둘수 있는 메모리를 할당
                //리터럴(literal)

    int y = x + 3;

    std::cout << y;

    return 0;
}